name: awesome_image
description: "A new Flutter package project."
version: 0.0.1
homepage: https://github.com/your_username/awesome_image

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  # ... rest of your flutter configuration
